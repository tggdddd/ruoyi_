<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.system.dal.mysql.user.AdminUserMapper">

    <select id="getOverRankPercentage"
            resultType="Integer">
        SELECT
        ROUND((COUNT(*) ) / (SELECT COUNT(*) FROM system_users) * 100, 2) AS rank_percentage
        FROM
        system_users
        WHERE
        create_time <![CDATA[ < ]]> (SELECT create_time FROM system_users WHERE id = 1);
</select>
    <select id="getNickName" resultType="java.lang.String">
        select nickname from system_users where id = #{id}
    </select>
    <select id="getRegisterTime" resultType="java.time.LocalDateTime">
        select create_time from system_users where id = #{id}
    </select>
</mapper>